{
  "config": {
    "step": {
      "user": {
        "title": "Power Manager",
        "description": "Initial setup (JSON import)."
      }
    },
    "error": {
      "invalid_json": "Invalid JSON."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Power Manager settings",
        "description": "Current config:\nBase load: {base_load}\nScan interval: {scan_interval}s\nProducers ({producer_count}): {producers}\nConsumers ({consumer_count}): {consumers}\n\nChoose an action, repeat as needed, then Save and close."
      },
      "base": {
        "title": "Update base settings"
      },
      "add_producer": {
        "title": "Add producer"
      },
      "update_producer_select": {
        "title": "Select producer"
      },
      "update_producer_edit": {
        "title": "Update producer"
      },
      "remove_producer": {
        "title": "Remove producer"
      },
      "add_consumer": {
        "title": "Add consumer"
      },
      "update_consumer_select": {
        "title": "Select consumer"
      },
      "update_consumer_edit": {
        "title": "Update consumer"
      },
      "remove_consumer": {
        "title": "Remove consumer"
      }
    },
    "error": {
      "name_exists": "Name already exists."
    }
  }
}
